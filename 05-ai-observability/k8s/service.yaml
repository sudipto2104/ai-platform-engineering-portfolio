apiVersion: v1
kind: Service
metadata:
  name: llm-metrics-exporter
  namespace: ai-observability
  labels:
    app: llm-metrics-exporter
spec:
  selector:
    app: llm-metrics-exporter
  ports:
    - name: metrics
      port: 8000
      targetPort: 8000
  type: ClusterIP